name: Selenium App Waker

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch: 

env:
  # This is the primary directive to tell the runner to ignore Node 20
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  wake_up:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Python
        uses: actions/setup-python@v5 # Stable version
        with:
          python-version: '3.10'

      - name: Install Selenium and Webdriver
        run: pip install selenium webdriver-manager

      - name: Run Waker
        run: |
          python -c "
          from selenium import webdriver
          from selenium.webdriver.chrome.options import Options
          from selenium.webdriver.common.by import By
          import time

          chrome_options = Options()
          chrome_options.add_argument('--headless')
          chrome_options.add_argument('--no-sandbox')
          chrome_options.add_argument('--window-size=1920,1080')
          driver = webdriver.Chrome(options=chrome_options)

          urls = ['https://frankneuro-fasta.streamlit.app/', 'https://frankneuro-primer.streamlit.app/']
          
          for url in urls:
              print(f'Navigating to {url}...')
              try:
                  driver.get(url)
                  time.sleep(15) 
                  
                  if 'Zzzz' in driver.page_source or 'Wake up' in driver.page_source:
                      print(f'Detected Sleep Page at {url}. Attempting to wake...')
                      button = driver.find_element(By.XPATH, '//button[contains(., \"back up\")]')
                      button.click()
                      print('Clicked wake button.')
                      time.sleep(5)
                  else:
                      print(f'App at {url} is already active.')
              except Exception as e:
                  print(f'Error visiting {url}: {e}')

          driver.quit()
          "

      - name: Upload Screenshot
        uses: actions/upload-artifact@v4 # Stable version
        with:
          name: streamlit-screenshot
          path: "*.png"
          if-no-files-found: ignore # Prevents the workflow from failing if no screenshot is taken
